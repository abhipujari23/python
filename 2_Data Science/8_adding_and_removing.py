#These functions allow us to add and remove elements from array

#np.resize(a, (x, y))
#returns a resized version of the array and fills empty spaces by repeating copies of a

#np.append(a, [...])
#appends calues at the end of the array

#np,insert(a, x, ...)
#insert a value at the index x of the value

#np.delete(a, x, y)
#delete axes of the array