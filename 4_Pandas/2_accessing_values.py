#accessing values works same as it works with dictionary, we address the respective index or key to get the value
from repeat import series

print("Using Index to access values")
print(series['C'])

print("\nUsing position to print values")
print(series[1])